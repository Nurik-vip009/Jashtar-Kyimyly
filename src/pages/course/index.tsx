import { CourseList } from "./section/CourseList";
import type { FC } from "react";

// Используйте заглавную букву для компонента
export const CoursePage: FC = () => {
	return (
		<>
			<CourseList />
		</>
	);
};
